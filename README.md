# Crystal Prompter AI Avatar Demo

Static demo for a floating AI avatar chatbot with product popups, voice input, and pre-recorded video replies.

## Run locally

Open `index.html` in a browser or run a static server:

```bash
python3 -m http.server 3000
```

Then visit: http://localhost:3000
